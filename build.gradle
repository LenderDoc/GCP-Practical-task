plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "LenderDoc_GCP-Practical-task"
    property "sonar.organization", "lenderdoc"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
